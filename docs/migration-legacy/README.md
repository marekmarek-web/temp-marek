# Legacy migration artifacts

Soubory z původního `migration/` adresáře — **referenční**, nejsou součástí buildu.

- `inventory.json` — mapování starých HTML tras na Next; FormSubmit toky jsou historické (dnes `POST /api/leads`).
- `QA_CHECKLIST.txt` — ruční kontrolní seznam při portu.

Aktivní procesy a návody jsou v `docs/*.md` (lead workflow, operations playbook, …).
